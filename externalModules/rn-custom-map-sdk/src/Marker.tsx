import { forwardRef } from 'react';
import type { MarkerMethods, MarkerProps } from './types';

const Marker = forwardRef<MarkerMethods, MarkerProps>(function Marker(_props, _ref) {
  return null;
});

Marker.displayName = 'RNCustomMapMarker';

export default Marker;
